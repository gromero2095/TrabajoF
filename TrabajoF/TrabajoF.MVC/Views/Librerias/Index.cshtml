@model IEnumerable<TrabajoF.Entities.Libreria>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LocalLibreria.Direccion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RUCLibreria)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LocalLibreria.Direccion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RUCLibreria)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Libreriaid }) |
            @Html.ActionLink("Details", "Details", new { id=item.Libreriaid }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Libreriaid })
        </td>
    </tr>
}

</table>

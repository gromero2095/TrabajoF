@model IEnumerable<TrabajoF.Entities.LocalLibreria>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Direccion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Direccion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LocalLibreriaid }) |
            @Html.ActionLink("Details", "Details", new { id=item.LocalLibreriaid }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LocalLibreriaid })
        </td>
    </tr>
}

</table>
